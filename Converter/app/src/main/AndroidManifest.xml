<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.application.converter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".CurrencyConverter"
            android:label="@string/title_activity_currency_converter"></activity>
        <activity
            android:name=".UnitWeight"
            android:label="@string/title_activity_unit_weight" />
        <activity
            android:name=".UnitLength"
            android:label="@string/title_activity_unit_length" />
        <activity
            android:name=".UnitArea"
            android:label="@string/title_activity_unit_area" />
        <activity
            android:name=".UnitConverter"
            android:label="@string/title_activity_unit_converter" />
        <activity
            android:name=".Standard_Cal"
            android:label="@string/title_activity_standard__cal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>